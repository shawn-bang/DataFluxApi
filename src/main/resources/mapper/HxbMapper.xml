<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="api.utils.HxbMapper">

	<select id="isExistsRequest" resultType="java.util.HashMap" parameterType="String" statementType="PREPARED">
		SELECT
			nvl(sum(app_id),0)
		  FROM request_applicantinfo
		 WHERE
			app_id = #{appId,jdbcType =VARCHAR}
	</select>

	<select id="selectAfsummary" resultType="java.util.HashMap" parameterType="String" statementType="PREPARED">
		SELECT
			type,
			value,
			remarks
		FROM response_afsummary
		WHERE
		app_id = #{appId,jdbcType =VARCHAR}
	</select>

	<select id="selectAfriskwarning" resultType="java.util.HashMap" parameterType="String" statementType="PREPARED">
		SELECT
			riskno,
			risktype,
			riskcategory,
			riskdesc
		FROM response_afriskwarning
		WHERE
		app_id = #{appId,jdbcType =VARCHAR}
	</select>

</mapper>